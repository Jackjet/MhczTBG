<UserControl x:Class="MhczTBG.Helper.DocumentControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" >
    <Border CornerRadius="5" BorderBrush="LightGray" BorderThickness="1">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.3*"/>
                <!--<ColumnDefinition />-->
                <ColumnDefinition Width="0.6*" />
            </Grid.ColumnDefinitions>
            <TreeView Background="Bisque" Name="treeView"/>
            <GridSplitter ShowsPreview="True" HorizontalAlignment="Right" Width="2" />
            
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="30"/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <TextBlock Name="txtTittle" VerticalAlignment="Center"/>
                <Border Name="bordMain" Grid.Row="1">
                   
                </Border>
            </Grid>
            

        </Grid>
    </Border>
</UserControl>
