<UserControl x:Class="MhczTBG.Controls.TimePicker"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="40" d:DesignWidth="300">
    <UserControl.Resources>
        
    </UserControl.Resources>
    <Grid Name="griMain">
        <Border Background="White"   Name="bor" BorderBrush="Silver" Visibility="Collapsed" BorderThickness="1"  VerticalAlignment="Top" Height="25" Margin="0,-25,0,0">
            <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
                <TextBlock Text="&lt;" />
                <TextBlock Name="txtUp" HorizontalAlignment="Center"/>
                <TextBlock Text=">" />
            </StackPanel>
        </Border>
        <TextBox Style="{x:Null}" Name="txtTime" HorizontalContentAlignment="Center" >
            
        </TextBox>    
    </Grid>
</UserControl>
